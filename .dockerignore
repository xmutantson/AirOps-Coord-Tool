# ---- VCS & meta ----
.git
.gitignore
.gitattributes
.github/

# ---- Editors/OS ----
.idea/
.vscode/
.DS_Store
Thumbs.db
# Note: .vscode/ scripts are excluded from Docker image (build happens outside container)

# ---- Python build/run cruft ----
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml

# ---- Env & local data (don’t bake secrets or local DBs into the image) ----
.env
.env.*
data/
*.db
*.sqlite*
*.sqlite3
*.log
reports/

# ---- Node/Frontend ----
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.tsbuildinfo

# ---- Backups & scratch ----
*.bak
*.bak.*
*.pre_fix.bak
*~
*.swp
*.swo

# ---- Dev-only stuff you don’t need in the image ----
tools/
scripts/
tests/
docs/
docker-compose.yml
docker-compose.*.yml

# (Everything else is allowed into the build context,
# including app.py, radio_tx.py, modules/, templates/, static/,
# airports.csv, english_adjectives.txt, graph.json, entrypoint.sh, etc.)
